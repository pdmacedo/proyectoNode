// const app = require("./server.js");

// const PORT = 8080;
// const server = app.listen(PORT, () => {
//     console.log(`Servidor en el puerto ${server.adress().port}`);
// })

// server.on('error', error => console.log(`Error en el servidor ${error}`));